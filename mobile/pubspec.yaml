name: p2p_marketplace
description: A Flutter P2P Marketplace client
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_riverpod: ^2.3.2
  # firebase_core: ^2.21.1
  # firebase_auth: ^4.6.3
  http: ^0.13.6
  dio: ^5.1.3
  cached_network_image: ^3.2.4
  flutter_secure_storage: ^9.0.0
  image_picker: ^0.8.7+3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
